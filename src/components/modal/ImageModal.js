import classes from './ImageModal.module.css'

const ImageModal = () => {
  return (
    <div className={classes.modal}>
      <p className={classes.text}>ImageModal!</p>
    </div>
  )
}

export default ImageModal
